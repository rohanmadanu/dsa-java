import java.util.*;

class Main {
    public static void main(String[] args) {
        int[] arr = {5, 2, 9, 1, 6};

        int n = arr.length;

        // Bubble Sort
        for (int i = 0; i < n - 1; i++) {
            for (int j = 0; j < n - i - 1; j++) {
                if (arr[j] > arr[j + 1]) {
                    // swap
                    int temp = arr[j];
                    arr[j] = arr[j + 1];
                    arr[j + 1] = temp;
                }
            }
        }

        // Print sorted array
        for (int x : arr) {
            System.out.print(x + " ");
        }
    }
}
